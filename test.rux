class TestComponent < ViewComponent::Base
  def initialize(title:, thing:)
    @title = title
    @thing = thing
  end

  def render
    <MyAwesomeForm className="form">
      <div class="form-section" data-track="foo" title={@title}>
        {5.times.map do
          <Button>Submit {@thing}</Button>
        end}
        <Button>Cancel</Button>
      </div>
      <Foo>bar</Foo>
    </MyAwesomeForm>
  end
end
